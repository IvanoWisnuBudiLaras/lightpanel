#!/usr/bin/env bash
set -u

cat <<'MESSAGE'
LightPanel prerequisite installer placeholder.

This script is intentionally inactive in Step 1.

Future Ubuntu setup may install:
- Rust toolchain
- Node.js for frontend build-time only
- SQLite tooling
- Nginx
- acme.sh
- ufw

Review every package and command before this script is made active.
No packages were installed.
MESSAGE

